# **Docker Setup**

## **Using Docker Compose**
1. **Build and run the container**

    ```bash
    docker-compose up --build

2. **Stop the container**

    ```bash
    docker-compose down


## **Access phpMyAdmin**

- Open http://localhost:8080 and log in using:
- Username: root
- Password: (from .env file)(my_sql_passcode)


